{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Servidor simulado usando JSON-server",
  "scripts": {
    "start": "json-server --watch ../mock-data/bd.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}